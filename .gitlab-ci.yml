image: nokeedev/generic-builder

build:
  stage: build
  script: gradle assemble

test:
  stage: test
  script: ./gradlew check

deploy:
  stage: deploy
  script: gradle bintrayUpload
  only:
    - master
